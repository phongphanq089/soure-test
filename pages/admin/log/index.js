import { Box, Typography } from '@mui/material';

const Log = () => {
    return (
        <Box sx={ { margin: 'auto', textAlign: 'center' } }>
            <Typography variant='h3'>
                Log
            </Typography>
        </Box>
    );
};

export default Log;