import { ContainerAdmin } from "@components/Layouts";
import { Typography } from "@mui/material";

const Menus = () => {

    return (
        <ContainerAdmin>
            <Typography variant="menus">menus</Typography>
        </ContainerAdmin>
    );
};

export default Menus;