import { Box, Typography } from '@mui/material';

const Free = () => {
    return (
        <Box sx={ { margin: 'auto', textAlign: 'center' } }>
            <Typography variant='h3'>
                Free
            </Typography>
        </Box>
    );
};

export default Free;